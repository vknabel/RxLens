# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RxLens' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RxLens
  pod 'RxSwift', '~> 3.0'

  target 'RxLensTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick', '~> 0.10'
    pod 'Nimble', '~> 5.1.0'
    pod 'RxTest', '~> 3.0'
    pod 'RxBlocking', '~> 3.0'
  end

end
